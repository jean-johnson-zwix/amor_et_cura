export default function SettingsPage() {
  return (
    <div className="flex flex-col gap-4">
      <h1 className="text-xl font-semibold">Settings</h1>
      <p className="text-sm text-muted-foreground">Admin settings — user roles, configurable fields — coming in P1 (issues #9, #20).</p>
    </div>
  )
}
